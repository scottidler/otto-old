otto:
  version: 1
  tasks:
    server:
      tasks:
        start:
          action: |
            #!/bin/bash
            echo "start server"
        stop:
          action: |
            #!/bin/bash
            echo "stop server"

