otto:
  version: 1
  jobs: 12
opts:
  o|option1:
    help: option1 help message
  O|option2:
    metavar: displayed_name
    dest: stored_name
    action: store # store_true, store_false, store_const, append, append_const, count
    nargs: + # *, ?
    const: const
    default: default
    type: type
    choices:
      - one
      - two
      - three
    required: false # true
    help: option2 help message
args:
  positional:
    metavar: displayed_name
    dest: stored_name
    action: store # store_true, store_false, store_const, append, append_const, count
    nargs: + # *, ?
    const: const
    default: default
    type: type
    choices:
      - one
      - two
      - three
    required: false # true
    help: help message
